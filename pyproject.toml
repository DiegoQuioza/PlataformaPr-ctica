[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "proyecto_automatización"
version = "0.1.0"

[tool.setuptools]
py-modules = ["paths"]

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-dir]
"automations" = "automations"